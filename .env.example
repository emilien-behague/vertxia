# Vertxia — API keys requises pour la génération 3D live
# Ne pas committer .env.local (déjà dans .gitignore)
# Sur Vercel : configurer ces variables dans Settings -> Environment Variables (Production)

# Meshy.ai — image-to-3D (UHQ meshy-6, 300k poly quad PBR)
# Plan Studio $99/mo : 4500 credits, ~30 credits par génération UHQ
# https://www.meshy.ai → Settings → API
MESHY_API_KEY=msy_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# Replicate — Real-ESRGAN AI upscale (nightmareai/real-esrgan)
# ~$0.005 par run, billed pay-as-you-go
# https://replicate.com → Account → API tokens
REPLICATE_API_TOKEN=r8_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
